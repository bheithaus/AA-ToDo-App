<%= form_for(@team) do |f| %>
<ul>
	<% @team.errors.full_messages.each do |error| %>
		<li><%= error %></li>
	<% end %>
</ul>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<br>
	<%= f.submit %>
<% end %>